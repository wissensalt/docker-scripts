services:
  database:
    image: "postgres:17.2-alpine3.21"
    container_name: postgresql_server
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql/data
    env_file:
      - database.env
